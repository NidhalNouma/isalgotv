name: django CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - run: cp ~/.env ./etradingiew
      - run: sudo docker build --tag isalgo .
      - run: sudo docker run --publish 80:8000 isalgo